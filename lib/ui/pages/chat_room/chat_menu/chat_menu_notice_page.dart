import 'package:flutter/material.dart';

class ChatRoomNoticePage extends StatelessWidget {
  const ChatRoomNoticePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("공지 화면"),
    );
  }
}
