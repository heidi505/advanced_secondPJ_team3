import 'package:flutter/material.dart';

class ChatRoomMediaPage extends StatelessWidget {
  const ChatRoomMediaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("미디어 화면"),
    );
  }
}
