import 'package:flutter/material.dart';

class ChatRoomNoticeSavePage extends StatelessWidget {
  const ChatRoomNoticeSavePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("공지 쓰기 화면"),
    );
  }
}
